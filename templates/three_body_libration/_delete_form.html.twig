<form method="post" action="{{ path('three_body_libration_delete', {'id': three_body_libration.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ three_body_libration.id) }}">
    <button class="btn">Delete</button>
</form>
